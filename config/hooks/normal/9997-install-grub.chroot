#!/bin/bash
set -e

if [ -d /sys/firmware/efi ]; then
  grub-install \
    --target=x86_64-efi \
    --efi-directory=/boot/efi \
    --bootloader-id=SavarezOS \
    --recheck \
    --no-nvram
else
  grub-install /dev/sda
fi

update-grub
